language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-zetam' ]; then cd .. && eval "mv $currentfolder generator-zetam" && cd generator-zetam; fi

